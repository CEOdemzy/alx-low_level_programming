#include "main.h"
/**
 * _isdgit - a function to check for digits 0-9
 * @c: describes integers to be checked
 * Return: 1 if c is a digit and 0 if otherwise
 */
int _isdigit(int c)
{
	return (c >= '0' && c <= '9');
}
