#include "main.h"
/**
 * mul - function tha multiply two integers
 * @a: a is a variable
 * @b: b is a variable
 * Return: Always 0
 */
int mul(int a, int b)
{
	int mul;

	mul = a* b;
	return (mul);
}
